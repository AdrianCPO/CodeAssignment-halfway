/*export const StatusSelector = ({ status, onChange }) => {
  return (
    <select value={status} onChange={e => onChange(e.target.value)}>
      <option value="open">Open</option>
      <option value="closed">Closed</option>
    </select>
  );
}; */
